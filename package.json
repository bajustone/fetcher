{
  "name": "@bajustone/fetcher",
  "version": "0.0.1",
  "description": "Schema-validated, typed fetch client with OpenAPI support",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "lint": "bunx --bun eslint .",
    "lint:fix": "bunx --bun eslint . --fix"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
